#!/bin/sh
set -euo pipefail

# Asume que tu script 'install' está junto al subdirectorio 'install/'
BASEDIR=$(cd "$(dirname "$0")" && pwd)
SCRIPTS_DIR="$BASEDIR/install"

echo "🔄 Ejecutando secuencialmente los scripts de instalación…"

i=0
while :; do
  prefix=$(printf "%03d" "$i")

  # Esta línea expande el patrón y pone las coincidencias en "$@"
  set -- "$SCRIPTS_DIR/${prefix}-"*
  # Si la primera posición no existe, no hay ningún fichero
  if [ ! -e "$1" ]; then
    echo
    echo "✅ Secuencia terminada en $prefix (no existe ${prefix}-*)"
    break
  fi

  # Ejecuta cada script que sí exista y sea ejecutable
  for script in "$SCRIPTS_DIR/${prefix}-"*; do
    [ -f "$script" ] || continue
    [ -x "$script" ] || { echo "⚠️ Saltando $script (no es ejecutable)"; continue; }
    echo
    echo "▶️  Ejecutando $script"
    "$script"
  done

  i=$((i + 1))
done

echo
echo "✅ Todos los scripts numerados han finalizado."
