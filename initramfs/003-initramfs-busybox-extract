#!/bin/sh
set -euo pipefail

source ./globals

echo "Unzipping BusyBox into initramfs output directory"
unzip -o "$BUSYBOX_DEPENDENCY_DIR/busybox.zip" -d "$INITRAMFS_OUTPUT_DIR/"
echo "BusyBox unzipped into initramfs output directory successfully"